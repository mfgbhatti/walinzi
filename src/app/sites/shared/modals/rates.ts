export interface ChargedRate{
  id: string;
  relative_id: string;
  type: string;
  charged_rate: number;
  status: boolean
}
export interface PayRate{
  id: string;
  relative_id: string;
  type: string;
  pay_rate: number;
  status: boolean
}

